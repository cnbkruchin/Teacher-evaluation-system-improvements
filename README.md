# ระบบประเมินผลการปฏิบัติงานครู v3.3

ระบบประเมินผลการปฏิบัติงานครู กลุ่มบริหารงานกิจการนักเรียน — พัฒนาต่อยอดจากระบบเดิม (v2)
ทำงานบน **Google Sheets + Google Apps Script** ใช้งานได้ทั้งบนคอมพิวเตอร์ แท็บเล็ต และมือถือ

> ⚠️ **อัปเกรดจากระบบเดิมได้ทันที** — ข้อมูลครู ผู้ประเมิน รหัสผ่าน และผลการประเมินเดิม
> จะถูกย้ายมาให้อัตโนมัติโดยไม่สูญหาย (ดู [docs/MIGRATION.md](docs/MIGRATION.md))

---

## สิ่งที่ปรับปรุงจากระบบเดิม

| ด้าน | ระบบเดิม (v2) | ระบบใหม่ (v3) |
|---|---|---|
| **หน้าจอ** | หน้าต่างซ้อนแยกกัน 4 หน้า | เว็บแอปเดียว มีเมนูข้าง รองรับมือถือ มีโหมดกลางคืน |
| **ความปลอดภัย** | SHA-256 รอบเดียว, salt ร่วมกันทั้งระบบ | salt เฉพาะราย + วนแฮช 4,096 รอบ, ล็อกบัญชีเมื่อเดารหัส, เซสชันหมดอายุ, บันทึกประวัติทุกการกระทำ |
| **ลืมรหัสผ่าน admin** | ไม่มี (ต้องแก้ชีทเอง) | 3 ช่องทาง: OTP ทางอีเมล / เมนูเจ้าของไฟล์ / รันฟังก์ชันฉุกเฉิน |
| **รายงาน** | สรุปลงชีทเท่านั้น | ส่งออก **Excel (.xlsx)** และ **PDF** พร้อมหัวรายงานและช่องลงนาม |
| **การส่งออก** | ส่งออกทั้งหมดเสมอ | **เลือกและจัดลำดับผู้ถูกประเมินเองก่อนประมวลผล** (ลากสลับตำแหน่งได้) |
| **เวรประจำวัน** | ผูกกับตัวครู 1 ค่า | **แยกอิสระตามภาคเรียน** เก็บบทบาทในเวร จุดปฏิบัติหน้าที่ และเวลา |
| **ข้อมูลย้อนหลัง** | ลบแล้วหายถาวร | **คลังข้อมูล** เก็บทุกฉบับที่ถูกแก้ไข/ลบ/ปิดภาคเรียน และกู้คืนได้ |
| **เกณฑ์การประเมิน** | แก้ได้เฉพาะในโค้ด | แก้ไข เพิ่ม ปิดใช้ และตั้งน้ำหนักได้จากหน้าจอ |
| **นำเข้ารายชื่อครู** | พิมพ์ทีละคนในชีท | อัปโหลดไฟล์ **Excel / CSV** พร้อมเทมเพลตให้โหลด ตรวจสอบก่อนบันทึกจริง |
| **การคิดคะแนน** | เฉลี่ยทุกการประเมินเท่ากันหมด | กำหนด **น้ำหนัก % ของผู้ประเมินแต่ละกลุ่ม** แล้วประมวลผลเป็น **คะแนนสุทธิ** |
| **ชุดของเกณฑ์** | มีชุดเดียวใช้ร่วมกันทั้งระบบ | แยกเป็น **ชุดประเมิน** ได้หลายชุด แต่ละชุดมีเกณฑ์ มาตราคะแนน และผู้ประเมินของตัวเอง |
| **คะแนนที่ส่งหน่วยงาน** | ต้องคิดเทียบสัดส่วนเอง | แปลง **คะแนนสุทธิ → คะแนนเต็มที่หน่วยงานกำหนด** อัตโนมัติ (เช่น 5.00 → 20 คะแนน) |
| **การติดตามงาน** | ดูความคืบหน้าได้อย่างเดียว | ติดตามรายชุด **แจ้งเตือนทางอีเมล** และ **ตรวจคุณภาพการประเมิน** ก่อนสรุปผล |
| **รอบการประเมิน** | เปิดตลอด แก้ไขย้อนหลังได้เสมอ | กำหนด **ช่วงวันที่เปิด-ปิด** และ **ล็อกภาคเรียนที่สรุปผลแล้ว** |
| **ปีการศึกษา/ภาคเรียน** | แก้ในโค้ด (ตัวเลือกตายตัว 4 ภาคเรียน) | เมนูเฉพาะ เพิ่ม/ลบปีการศึกษา เปิด-ปิดภาคเรียน (รวมภาคฤดูร้อน) ตั้งค่าปัจจุบันได้อิสระ |
| **เคลียร์ข้อมูล** | ลบทีละรายการเท่านั้น | เคลียร์รายภาคเรียน/รายปี/ทั้งหมด เลือกย้ายเข้าคลังหรือลบถาวร พร้อมสำรองเป็น Excel |
| **ความเร็ว** | อ่าน-เขียนชีทซ้ำหลายรอบต่อคำขอ | ลดการคุยกับ Google Sheets ลง 76% (ดู [PERFORMANCE.md](docs/PERFORMANCE.md)) |
| **หน้าจอบนอุปกรณ์** | ตายตัว มีแถบเลื่อนซ้อน | เต็มหน้าจอทุกอุปกรณ์ เมนูปรับตามขนาดจอ ตารางเป็นการ์ดบนมือถือ |
| **คะแนน** | เก็บเป็นสูตรในชีท (พังเมื่อย้ายข้อมูล) | คำนวณและเก็บเป็นค่าคงที่ ตรวจสอบย้อนหลังได้เสมอ |

---

## ความสามารถหลัก

### สำหรับผู้ดูแลระบบ
- **แดชบอร์ด** — สถิติภาพรวม กราฟ 4 ชุด ความคืบหน้าของผู้ประเมินรายคน อันดับคะแนน
- **ผลการประเมิน** — ค้นหา/กรองตามปี ภาคเรียน ระดับชั้น วันเวร ดูคะแนนรายข้อและประวัติการแก้ไข
- **ประมวลผลและส่งออก** — เลือกผู้ถูกประเมิน จัดลำดับเอง (ลาก/ปุ่มเลื่อน) หรือใช้การเรียงอัตโนมัติ
  → ดูตัวอย่างก่อน → ส่งออก Excel / PDF / ทั้งสองแบบ / **รายงานรายบุคคล (หน้าละ 1 คน)**
- **คลังข้อมูลย้อนหลัง** — ปิดภาคเรียน ค้นหาข้อมูลเก่า และกู้คืนกลับได้
- **ข้อมูลหลัก** — ครู, ผู้ประเมิน (สุ่มรหัสผ่าน + ส่งอีเมลอัตโนมัติ),
  เวรประจำวันรายภาคเรียน, เกณฑ์การประเมิน
- **นำเข้ารายชื่อครูจากไฟล์** — อัปโหลด Excel (.xlsx .xls .ods) หรือ CSV/TSV ลากไฟล์มาวางได้
  มีเทมเพลตให้ดาวน์โหลด จับคู่คอลัมน์อัตโนมัติ และแสดงตัวอย่างให้ตรวจก่อนบันทึก
- **ชุดประเมิน** — แยกเกณฑ์ มาตราคะแนน และกลุ่มผู้ประเมินออกเป็นชุด ๆ ได้อิสระ
  แต่ละชุดกำหนดได้ว่าจะแปลงเป็นคะแนนเต็มเท่าไรของหน่วยงาน สร้าง/คัดลอก/ปิดใช้/ลบได้จากหน้าจอ
- **น้ำหนักกลุ่มผู้ประเมิน** — กำหนดว่าคะแนนของผู้ประเมินแต่ละกลุ่มคิดเป็นกี่ % ของคะแนนรวม
  ระบบประมวลผลเป็น "คะแนนสุทธิ" แล้วแปลงเป็น "คะแนนที่หน่วยงานได้รับ" พร้อมดูผลกระทบก่อนบันทึก
- **ติดตามความคืบหน้า** — เห็นทันทีว่าผู้ประเมินคนใดค้างชุดใดกี่รายการ ครูคนใดยังขาดการประเมินจากกลุ่มใด
  พร้อมส่งอีเมลแจ้งเตือนเฉพาะคนที่ยังทำไม่ครบ
- **ตรวจคุณภาพการประเมิน** — จับสัญญาณที่ทำให้ผลไม่สะท้อนความจริง เช่น ให้คะแนนเท่ากันทุกข้อ
  ให้คะแนนสูง/ต่ำกว่าค่าเฉลี่ยของระบบอย่างชัดเจน หรือให้คะแนนครูทุกคนแทบไม่ต่างกัน
- **รอบการประเมิน** — เปิด/ปิดการบันทึกผล กำหนดช่วงวันที่ และล็อกภาคเรียนที่สรุปผลไปแล้ว
- **ปีการศึกษาและภาคเรียน** — เพิ่ม/ลบปีการศึกษา เปิด-ปิดภาคเรียน (รองรับภาคฤดูร้อน)
  เลือกปีและภาคเรียนปัจจุบันแยกจากกัน พร้อมตารางสรุปจำนวนข้อมูลของทุกภาคเรียน
- **เคลียร์ข้อมูลการประเมิน** — ล้างข้อมูลรายภาคเรียน รายปีการศึกษา หรือทั้งหมด
  เลือกได้ว่าจะย้ายเข้าคลัง (กู้คืนได้) หรือลบถาวร พร้อมสำรองเป็นไฟล์ Excel ก่อนลบ
- **ตั้งค่าและความปลอดภัย** — เกณฑ์ตัดระดับ อีเมลกู้คืนรหัสผ่าน นโยบายเซสชัน
- **ประวัติการใช้งาน** — บันทึกทุกการเข้าสู่ระบบและการแก้ไขข้อมูล

### หน้าจอ
- **เต็มหน้าจอเสมอ** เลื่อนเฉพาะส่วนเนื้อหา ไม่มีแถบเลื่อนซ้อนกัน
- **คอมพิวเตอร์** เมนูข้างเต็มรูปแบบ · **แท็บเล็ต** เมนูย่อเป็นแถบไอคอน ·
  **มือถือ** เมนูลิ้นชัก + แถบเมนูล่าง และตารางแสดงเป็นการ์ดอ่านง่าย
- โหมดสว่าง/มืด, ปุ่มเต็มหน้าจอ, รองรับจอมีติ่ง (safe area) และการกดด้วยนิ้ว (ปุ่มขั้นต่ำ 44px)

### สำหรับผู้ประเมิน
- เห็นเฉพาะครูและเกณฑ์ตามสิทธิ์ของตนเอง (รอง ผอ. / หัวหน้ากลุ่มฯ / หัวหน้าระดับชั้น / หัวหน้าเวร)
- **เลือกชุดประเมิน**ที่ได้รับมอบหมายได้เอง หน้าจอปรับมาตราคะแนนตามชุดโดยอัตโนมัติ
- แบบประเมินแสดงความคืบหน้า มีปุ่มให้คะแนนรวดเร็ว ค้นหาชื่อครูได้
- แก้ไขผลที่เคยบันทึกได้ — ระบบเก็บฉบับเดิมไว้ในคลังข้อมูลเสมอ
- ดูตารางเวรประจำวันของภาคเรียน และเปลี่ยนรหัสผ่านด้วยตนเอง

---

## เริ่มต้นใช้งานอย่างรวดเร็ว

1. เปิดไฟล์ Google Sheets ของระบบ → เมนู **ส่วนขยาย (Extensions) → Apps Script**
2. คัดลอกไฟล์ทั้งหมดในโฟลเดอร์ [`src/`](src) เข้าโปรเจกต์ (ดูวิธีละเอียดใน [docs/INSTALL.md](docs/INSTALL.md))
3. กลับมาที่ Google Sheets → รีเฟรชหน้า → เมนู **🏫 ระบบประเมินผล → ⚙️ ติดตั้ง / อัปเกรดระบบ**
4. จดรหัสผ่านผู้ดูแลระบบที่ระบบแสดงให้
5. เมนู **🏫 ระบบประเมินผล → 🚀 เปิดระบบ** → เข้าสู่ระบบผู้ดูแล
6. ไปที่ **ตั้งค่าและความปลอดภัย** → กรอก **อีเมลกู้คืนรหัสผ่าน** แล้วกดทดสอบส่งอีเมล
7. เพิ่มรายชื่อครู → ผู้ประเมิน → ตารางเวรประจำวัน แล้วส่งลิงก์เว็บแอปให้ผู้ประเมิน

---

## โครงสร้างโปรเจกต์

```
src/                      โค้ดที่ใช้งานจริง (อัปโหลดขึ้น Apps Script)
├── appsscript.json       สิทธิ์การเข้าถึงและการตั้งค่าเว็บแอป
├── 00_Config.gs          ค่าคงที่ ชื่อชีท บทบาท เกณฑ์เริ่มต้น
├── 01_Data.gs            อ่าน/เขียนชีทโดยอ้างอิงชื่อคอลัมน์
├── 02_Security.gs        เข้ารหัสรหัสผ่าน เซสชัน จำกัดการล็อกอิน
├── 03_Setup.gs           ติดตั้ง/อัปเกรด และย้ายข้อมูลจากระบบเดิม
├── 04_Domain.gs          ชุดประเมิน เกณฑ์ การคำนวณคะแนน เวรประจำวันรายภาคเรียน
├── 05_Auth.gs            เข้าสู่ระบบ/ออกจากระบบ/เปลี่ยนรหัสผ่าน
├── 06_Recovery.gs        กู้คืนรหัสผ่านผู้ดูแลระบบ (3 ช่องทาง)
├── 07_EvaluatorApi.gs    ฟังก์ชันสำหรับผู้ประเมิน
├── 08_AdminApi.gs        ฟังก์ชันสำหรับผู้ดูแลระบบ
├── 09_Reports.gs         ประมวลผลและส่งออก Excel/PDF
├── 10_Archive.gs         คลังข้อมูลย้อนหลัง
├── 11_WebApp.gs          จุดเริ่มต้นเว็บแอปและเมนู
├── 12_SelfTest.gs        ทดสอบตรรกะระบบจาก Apps Script Editor
├── 13_Maintenance.gs     ปีการศึกษา/ภาคเรียน และการเคลียร์ข้อมูลการประเมิน
├── 14_Scoring.gs         น้ำหนักกลุ่มผู้ประเมินและคะแนนสุทธิ
├── 15_Import.gs          นำเข้ารายชื่อครูจากไฟล์ Excel/CSV และเทมเพลต
├── 16_Sets.gs            ชุดประเมินและกลุ่มผู้ประเมินของแต่ละชุด
├── 17_Operations.gs      ติดตามความคืบหน้า แจ้งเตือน ตรวจคุณภาพ และรอบการประเมิน
├── Index.html            โครงหน้าจอ
├── Styles.html           ชุดรูปแบบ (รองรับโหมดสว่าง/มืด)
├── AppJs.html            แกนกลาง: เรียก API, เซสชัน, แจ้งเตือน
├── AdminJs.html          แดชบอร์ด ผลการประเมิน คลังข้อมูล ประวัติ
├── AdminDataJs.html      ครู ผู้ประเมิน เวรประจำวัน เกณฑ์ ตั้งค่า
├── AdminExportJs.html    เลือก/จัดลำดับ/ประมวลผล/ส่งออก
├── AdminTermsJs.html     ปีการศึกษา/ภาคเรียน และการเคลียร์ข้อมูล
├── AdminScoreJs.html     น้ำหนักกลุ่มผู้ประเมินและการแปลงคะแนน
├── AdminSetsJs.html      ชุดประเมินและกลุ่มผู้ประเมิน
├── AdminOpsJs.html       ติดตามความคืบหน้า ตรวจคุณภาพ และรอบการประเมิน
└── EvaluatorJs.html      หน้าจอผู้ประเมิน

docs/                     คู่มือการติดตั้ง ใช้งาน ความปลอดภัย และการย้ายข้อมูล
tests/                    ชุดทดสอบอัตโนมัติ (รันด้วย Node.js)
legacy/                   ซอร์สโค้ดระบบเดิม (v2) เก็บไว้อ้างอิง
```

## ชีทข้อมูลในระบบ

| ชีท | เก็บอะไร |
|---|---|
| `รายชื่อครู` | ทะเบียนครูผู้รับการประเมิน |
| `ผู้ประเมิน` | บัญชีผู้ประเมิน (ซ่อนไว้ — มีค่าแฮชรหัสผ่าน) |
| `ชุดประเมิน` | ชุดของเกณฑ์ มาตราคะแนนต่อข้อ และคะแนนเต็มที่หน่วยงานได้รับ |
| `กลุ่มผู้ประเมินของชุด` | กลุ่มผู้ประเมินและน้ำหนัก % ของแต่ละชุด (อิงบทบาทหรือรายบุคคล) |
| `เกณฑ์การประเมิน` | เกณฑ์ ผู้มีสิทธิ์ประเมิน และน้ำหนัก (แยกตามชุด) |
| `เวรประจำวันรายภาคเรียน` | ตารางเวร แยกตามปีการศึกษา/ภาคเรียน |
| `ผลการประเมิน` | ผลการประเมินที่ใช้งานอยู่ |
| `คลังผลการประเมิน` | ข้อมูลย้อนหลังทั้งหมด (ฉบับแก้ไข/ถูกลบ/ปิดภาคเรียน) |
| `สรุปผลการประเมิน` | ผลสรุปรายบุคคลที่สั่งประมวลผล |
| `ประวัติการใช้งาน` | บันทึกการใช้งานระบบ (audit log) |
| `ตั้งค่าระบบ` | การตั้งค่าและรหัสผ่านผู้ดูแล (ซ่อนไว้) |

---

## ชุดประเมินและการคิดคะแนน

**ชุดประเมิน** คือกล่องที่บรรจุเกณฑ์ มาตราคะแนน และกลุ่มผู้ประเมินของตัวเอง
โรงเรียนจึงแยกการประเมินออกเป็นชุด ๆ ได้อิสระ เช่น

| ชุด | เกณฑ์ | เต็มต่อข้อ | คะแนนที่หน่วยงานได้รับ |
|---|---|---|---|
| งานกิจการนักเรียน | 10 ข้อ | 5 | 20 คะแนน |
| งานวิชาการ | 8 ข้อ | 4 | 30 คะแนน |

ผู้ประเมินของแต่ละชุดกำหนดเป็น "กลุ่ม" ได้ 2 แบบ
**แบบบทบาท** (ทุกคนที่มีบทบาทนั้น) และ **แบบรายบุคคล** (เลือกรายคน — มีสิทธิ์เหนือกว่าแบบบทบาท)

### ลำดับการคิดคะแนนของครู 1 คน ในชุด 1 ชุด

```
1. คะแนนเฉลี่ยของกลุ่ม   เฉลี่ยคะแนนที่ผู้ประเมินในกลุ่มเดียวกันให้
2. คะแนนสุทธิ            Σ (คะแนนเฉลี่ยของกลุ่ม × น้ำหนักของกลุ่ม) ÷ Σ น้ำหนักที่ใช้จริง
3. คะแนนที่หน่วยงานได้รับ  คะแนนสุทธิ ÷ คะแนนเต็มต่อข้อ × คะแนนเต็มที่หน่วยงานกำหนด
```

**ตัวอย่าง** ชุดงานกิจการนักเรียน (เต็มข้อละ 5 · หน่วยงานให้ 20 คะแนน)
กำหนด รอง ผอ. 40% · หน.กิจการฯ 30% · หน.ระดับชั้น 20% · หน.เวรฯ 10%
ครูได้คะแนนเฉลี่ยจากแต่ละกลุ่ม 5.00, 4.00, 4.50, 3.00 ตามลำดับ

- คะแนนสุทธิ = (5.00×40 + 4.00×30 + 4.50×20 + 3.00×10) ÷ 100 = **4.40**
- คะแนนที่หน่วยงานได้รับ = 4.40 ÷ 5 × 20 = **17.60 คะแนน**

หากมีหลายชุด ระบบจะรวมคะแนนที่ได้รับของทุกชุดเป็นคะแนนรวมของครูแต่ละคน

การถ่วงน้ำหนักเปิด/ปิดได้รายชุดที่เมนู **⚖️ น้ำหนักผู้ประเมิน** — ถ้าปิดจะใช้ค่าเฉลี่ยรวมตามปกติ
และหากครูไม่ได้รับการประเมินจากบางกลุ่ม ระบบจะ**ปรับสัดส่วนอัตโนมัติ**
(หารด้วยน้ำหนักเฉพาะกลุ่มที่ประเมินจริง) เพื่อไม่ให้เสียเปรียบ — ปิดตัวเลือกนี้ได้

## รูปแบบรายงาน

| รายงาน | ใช้เมื่อไร | สิ่งที่ได้ |
|---|---|---|
| **สรุปผลการประเมิน** | ส่งผู้บริหาร / เก็บเป็นหลักฐาน | ตารางครูทุกคน คะแนนรายกลุ่มผู้ประเมิน คะแนนสุทธิ คะแนนที่ได้รับ ระดับผล |
| **คะแนนรายชุดประเมิน** | กรอกคะแนนส่งหน่วยงาน | ตารางครู × ชุดประเมิน แสดงคะแนนที่ได้รับของแต่ละชุด คะแนนรวม และร้อยละ |
| **คะแนนรายข้อ** | วิเคราะห์จุดแข็ง-จุดอ่อน | คะแนนเฉลี่ยรายข้อของครูทุกคน พร้อมคำอธิบายเลขข้อ |
| **รายงานรายบุคคล** | แจ้งผลให้ครูรายคน | 1 คน 1 หน้า มีข้อมูลครู คะแนนทุกชุด คะแนนรายข้อ ข้อเสนอแนะ และช่องลงนาม |
| **ข้อเสนอแนะ / รายการประเมินทั้งหมด** | ตรวจสอบย้อนหลัง | ข้อเสนอแนะทุกข้อ และรายการประเมินดิบทั้งหมด |

ทุกรายงานส่งออกเป็น **Excel (.xlsx)** และ **PDF** ได้ และบันทึกไว้ใน Google Drive
โฟลเดอร์ `รายงานผลการประเมินครู` โดยอัตโนมัติ

## การทดสอบ

```bash
bash tests/run-all.sh      # ต้องมี Node.js 18 ขึ้นไป
```

ชุดทดสอบจำลอง Google Apps Script ทั้งหมดไว้ในหน่วยความจำ จึงรันได้โดยไม่ต้องเชื่อมต่อ Google
ครอบคลุมการติดตั้ง สิทธิ์ตามบทบาท การคำนวณคะแนน เวรรายภาคเรียน คลังข้อมูล การส่งออก
ความปลอดภัย การกู้คืนรหัสผ่าน การตั้งค่าปีการศึกษา การเคลียร์ข้อมูล
การนำเข้าไฟล์ Excel/CSV การคิดคะแนนสุทธิแบบถ่วงน้ำหนัก ชุดประเมินและการแปลงคะแนน
การติดตามความคืบหน้า การแจ้งเตือน การตรวจคุณภาพ รอบการประเมิน
และการย้ายข้อมูลจากระบบเดิม (รวม **312 รายการ**)

วัดความเร็วของระบบได้ด้วย

```bash
node tests/benchmark.js
```

ตรวจสอบระบบที่ติดตั้งจริงได้จากเมนู **🩺 ตรวจสุขภาพระบบ**
หรือรันฟังก์ชัน `runSelfTest()` ใน Apps Script Editor

---

## เอกสารเพิ่มเติม

- 📕 **[คู่มือการใช้งานสำหรับครูผู้ประเมิน (PDF พร้อมแจก)](docs/คู่มือการใช้งานสำหรับครูผู้ประเมิน.pdf)**
  — 14 หน้า A4 ภาษาไทย มีภาพหน้าจอประกอบ พร้อมภาคผนวกสรุปสำหรับผู้ดูแลระบบ
  (ไฟล์ต้นฉบับแก้ไขได้ที่ [`docs/manual/`](docs/manual))
- [คู่มือการติดตั้ง](docs/INSTALL.md)
- [คู่มือการใช้งาน](docs/USER-GUIDE.md)
- [ความปลอดภัยและการกู้คืนรหัสผ่าน](docs/SECURITY.md)
- [การย้ายข้อมูลจากระบบเดิม](docs/MIGRATION.md)
- [ประสิทธิภาพและความเร็ว](docs/PERFORMANCE.md)
- [ประวัติการเปลี่ยนแปลง](docs/CHANGELOG.md)
